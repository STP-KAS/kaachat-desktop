node_modules/
.rusty-build/
dist/
*.log
.DS_Store
vendor/kasia-cipher/target/
.claude/

# Build-time secrets (ChangeNOW key etc.) — inlined into the bundle by Vite, never committed.
# Mirrors iOS Secrets.xcconfig / Android local.properties. See .env.example.
.env
.env.local
.env.*.local
